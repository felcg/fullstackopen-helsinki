const message = 'This is an notification'

const notificationReducer = (state = 'NOTIFICATION', action) => {
    switch (action.type) {
    default:
      return message
    }
  }

  export default notificationReducer
  